[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd dashboard && npm ci"]

[phases.build]
cmds = ["cd dashboard && npm run build"]

[start]
cmd = "cd dashboard && npm run preview -- --host 0.0.0.0 --port ${PORT:-3000}"
